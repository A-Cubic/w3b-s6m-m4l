import { queryRule, removeRule, addRule, updateRule } from '@/services/api';

export default {
  namespace: 'promotionAreaModel',

  state: {
    data: {
      list: [],
      pagination: {},
    },
  },

  effects: {},

  reducers: {},
};
